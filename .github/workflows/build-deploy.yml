name: Build and Deploy Code

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  job1:
    env:
      SECRET_KEY: "551f1025bba9fe2bb419ec2cfac54d0cdcd9ace897c52178ff7b5025cc1a651e"
      ALGORITHM: "HS256"
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      DATABASE_URL: "postgresql://postgres:bertin@localhost:5433/fastapi"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Install Python version 3.10
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      - name: Update pip 
        run: python -m pip install --upgrade pip
      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: |
          pip install pytest
          pytest

      
    
